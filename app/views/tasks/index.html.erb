<h1>タスク一覧</h1>
<p>今日も一日タスクを管理しましょう！</p>
<%= link_to "新規登録", new_task_path %>

<% @task.each do |task| %>
    <div>
        <p>
            <%= task.name %><br>
            <%= task.description %>
        </p>
        <p>
            <%= link_to "詳細", task_path(task) %>
            <%= link_to "編集", edit_task_path(task) %>
            <%= link_to "削除", task_path(task), method: :delete, data: {confirm: "本当に削除しますか？"} %>
        </p>
        <hr>
    </div>
<% end %>